import { RoleStyleDirective } from './role-style.directive';

describe('RoleStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleStyleDirective();
    expect(directive).toBeTruthy();
  });
});
